# Agent Rules for PORTFOLIO

- **Auto-Commit and Push**: Whenever the user asks you to update, change, or fix anything in this project, you must automatically run `git add .`, `git commit -m "<descriptive message>"`, and `git push` to the repository after completing the requested changes, without asking for explicit permission.
